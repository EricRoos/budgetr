var selector = "#itemEditModal<%=@item.id%>";
$(selector).modal('hide');
$('.toast .toast-body').html("Successfully edited")
$('.toast').toast('show');
$("#itemEditModal<%= @item.id%>").html("<%=j render partial: 'items/modal_form', locals: { item: @item } %>")
