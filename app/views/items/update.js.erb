$("#itemEditModal<%=@item.id%>").on('hidden.bs.modal', function() {
  $("div[data-item-group-id='<%= @item.item_group.id%>']").html("<%=j render @item.item_group %>");
});
$("#itemEditModal<%=@item.id%>").modal('hide');
